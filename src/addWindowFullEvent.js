import React, { Component } from 'react'

//addButton
class addWindowFullEvent extends Component {

    render() {
        return (
            <div onClick = {this.props.getWindow} className="addCalendar"></div>
        )}
}

export default addWindowFullEvent



